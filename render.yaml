services:
  - type: web
    name: bingobirr-api
    env: node
    rootDir: backend
    buildCommand: yarn install && yarn prisma generate && yarn prisma migrate deploy
    startCommand: yarn start
    plan: free
    envVars:
      - key: NODE_ENV
        value: production
      - key: ALLOWED_ORIGINS
        value: "*"
